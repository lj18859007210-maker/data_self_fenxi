services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - data:/app/data
    environment:
      - POLARS_MAX_THREADS=4

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  data:
